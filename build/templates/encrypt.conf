server {
  listen 80;

  server_name _SERVERNAME_;

  location / {
      root /var/empty/nginx;
  }
}

server {
  listen 443;

  server_name _SERVERNAME_;

  location / {
      root /var/empty/nginx;
  }
}
